---
// Sentinel - Sidebar Auditor Pro (Estructura Blindada)
---
<div class="fixed top-24 right-0 z-[1100] flex items-start pointer-events-none">
    
    <button 
        id="sidebar-trigger"
        class="pointer-events-auto bg-[#161a1e] border-2 border-[#F3BA2F]/40 border-r-0 p-3.5 rounded-l-2xl shadow-[-10px_0_30px_rgba(243,186,47,0.2)] transition-all hover:scale-105 group border-l-[5px] border-l-[#F3BA2F]"
    >
        <i class="fas fa-microchip text-[#F3BA2F] text-sm group-hover:rotate-12 transition-transform" id="trigger-icon"></i>
        <div class="absolute inset-0 bg-gradient-to-r from-[#F3BA2F]/10 to-transparent opacity-0 group-hover:opacity-100 transition-opacity"></div>
    </button>

    <aside 
        id="sidebar-panel" 
        class="pointer-events-auto w-0 overflow-hidden bg-[#0b0e11]/98 backdrop-blur-3xl border-l border-white/10 transition-all duration-300 ease-in-out shadow-[-30px_0_80px_rgba(0,0,0,0.9)] rounded-l-[2rem]"
    >
        <div class="w-72 p-6 h-full overflow-y-auto custom-scroll flex flex-col">
            <div class="flex items-center justify-between mb-8">
                <div>
                    <p class="text-[11px] font-black text-[#F3BA2F] uppercase tracking-[0.2em]">Sentinel Auditor</p>
                    <p class="text-[7px] text-gray-500 uppercase font-bold tracking-widest mt-0.5 italic">Integrity Monitor</p>
                </div>
                <span class="p-2 bg-emerald-500/10 rounded-lg shadow-[0_0_10px_rgba(16,185,129,0.2)]">
                    <i class="fas fa-shield-check text-emerald-500 text-[10px]"></i>
                </span>
            </div>

            <div class="space-y-4 mb-8">
                <div class="bg-white/[0.03] p-3 rounded-xl border border-white/5 relative overflow-hidden group">
                    <span class="text-[8px] text-gray-500 uppercase font-black block mb-1">Balance Teórico Sistema</span>
                    <p id="side-teorico" class="text-sm font-mono font-black text-white tracking-tighter">$0.00</p>
                </div>

                <div class="bg-emerald-500/5 p-3 rounded-xl border border-emerald-500/10 border-l-2 border-emerald-500/40">
                    <span class="text-[8px] text-emerald-500/60 uppercase font-black block mb-1">Binance Wallet (API)</span>
                    <p id="side-binance" class="text-sm font-mono font-black text-emerald-400 tracking-tighter">$0.00</p>
                </div>

                <div class="bg-white/[0.03] p-3 rounded-xl border border-white/5">
                    <span class="text-[8px] text-rose-500 uppercase font-black block mb-1 italic tracking-widest">Discrepancia (A)</span>
                    <p id="side-discrepancia" class="text-sm font-mono font-black tracking-tighter text-gray-400">$0.00</p>
                </div>

                <div class="bg-emerald-500/10 p-3 rounded-xl border border-emerald-500/20 shadow-[inset_0_0_15px_rgba(16,185,129,0.05)] border-l-2 border-emerald-500">
                    <span class="text-[8px] text-emerald-400 uppercase font-black block mb-1">Profit Generado Real</span>
                    <p id="side-profit-total" class="text-sm font-mono font-black text-white tracking-tighter">$0.00</p>
                </div>

                <div class="bg-blue-500/5 p-3 rounded-xl border border-blue-500/10 flex justify-between items-center group">
                    <span class="text-[8px] text-blue-400 uppercase font-black italic">Avg / Ciclo</span>
                    <span id="side-avg-ciclo" class="text-xs font-mono font-bold text-white group-hover:text-blue-400 transition-colors">$0.00</span>
                </div>
            </div>

            <div class="flex-1">
                <div class="flex items-center justify-between mb-4 border-b border-white/10 pb-2">
                    <p class="text-[9px] font-black text-gray-400 uppercase tracking-widest italic">Monitor Bancos</p>
                    <div class="flex items-center gap-1">
                        <span class="text-[7px] text-gray-600 font-bold uppercase">Meta: 1k</span>
                    </div>
                </div>
                <div id="side-banks-list" class="space-y-3">
                    </div>
            </div>
        </div>
    </aside>
</div>

<style>
    .custom-scroll::-webkit-scrollbar { width: 3px; }
    .custom-scroll::-webkit-scrollbar-thumb { background: #F3BA2F33; border-radius: 10px; }
    #sidebar-panel { height: calc(100vh - 8rem); margin-top: 1rem; }
</style>

<script>
    const trigger = document.getElementById('sidebar-trigger');
    const panel = document.getElementById('sidebar-panel');
    const icon = document.getElementById('trigger-icon');
    let isOpen = false;

    trigger?.addEventListener('click', (e) => {
        e.stopPropagation();
        isOpen = !isOpen;
        panel.style.width = isOpen ? '288px' : '0'; 
        icon.className = isOpen ? 'fas fa-times text-[#F3BA2F] text-[10px]' : 'fas fa-microchip text-[#F3BA2F] text-[10px]';
    });

    document.addEventListener('click', (e) => {
        if (isOpen && !panel.contains(e.target) && !trigger.contains(e.target)) {
            isOpen = false;
            panel.style.width = '0';
            icon.className = 'fas fa-microchip text-[#F3BA2F] text-[10px]';
        }
    });
</script>