---
// src/components/Training.astro
const tracks = [
  {
    title: "Masterclass P2P Pro",
    tag: "Educación",
    description: "De principiante a experto. Aprende las estrategias exactas que usamos para operar con seguridad y alta rentabilidad.",
    features: ["Gestión de riesgos", "Evasión de estafas", "Optimización de spreads"],
    icon: "fa-graduation-cap",
    color: "from-blue-500/20 to-transparent"
  },
  {
    title: "Operadores Delegados",
    tag: "Gestión",
    description: "Ponemos a tu disposición nuestro equipo experto para gestionar tu capital, maximizando resultados sin que tengas que operar.",
    features: ["Operaciones 24/7", "Reportes diarios", "Capital protegido"],
    icon: "fa-user-tie",
    color: "from-[#F3BA2F]/20 to-transparent"
  },
  {
    title: "Mesa de Trading",
    tag: "Inversión",
    description: "Participa directamente en nuestro fondo de liquidez P2P. Operamos con capital propio y de socios estratégicos.",
    features: ["Liquidez inmediata", "Alta rotación", "Transparencia total"],
    icon: "fa-chart-line",
    color: "from-green-500/20 to-transparent"
  }
];
---

<section id="training" class="py-24 bg-[#0b0e11] relative overflow-hidden">
    <div class="max-w-7xl mx-auto px-6">
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-6xl font-black text-white mb-4 uppercase tracking-tighter">
                Expandiendo tu <span class="text-[#F3BA2F]">Potencial</span>
            </h2>
            <p class="text-gray-400 max-w-2xl mx-auto text-lg">
                Más que una herramienta, somos un ecosistema completo para dominar el mercado P2P.
            </p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {tracks.map(item => (
                <div class={`relative bg-[#1e2329] border border-gray-800 p-8 rounded-3xl hover:border-[#F3BA2F]/50 transition-all group overflow-hidden flex flex-col`}>
                    <div class={`absolute inset-0 bg-gradient-to-br ${item.color} opacity-0 group-hover:opacity-100 transition-opacity`}></div>
                    
                    <div class="relative z-10 flex-grow">
                        <span class="text-[10px] font-bold tracking-[0.2em] text-[#F3BA2F] uppercase bg-[#F3BA2F]/10 px-3 py-1 rounded-full mb-6 inline-block">
                            {item.tag}
                        </span>
                        
                        <div class="text-3xl text-white mb-6 group-hover:scale-110 transition-transform duration-500">
                            <i class={`fas ${item.icon}`}></i>
                        </div>
                        
                        <h3 class="text-2xl font-bold text-white mb-4 uppercase tracking-tight">{item.title}</h3>
                        <p class="text-gray-400 text-sm leading-relaxed mb-6 italic">
                            "{item.description}"
                        </p>
                        
                        <ul class="space-y-3 mb-8">
                            {item.features.map(feat => (
                                <li class="text-gray-300 text-xs flex items-center gap-2">
                                    <i class="fas fa-check text-[#F3BA2F]"></i> {feat}
                                </li>
                            ))}
                        </ul>
                    </div>

                    <a href="https://wa.me/TU_NUMERO" class="relative z-10 w-full py-4 bg-white/5 border border-white/10 text-white rounded-xl font-bold text-center group-hover:bg-[#F3BA2F] group-hover:text-black transition-all uppercase text-xs tracking-widest">
                        Solicitar Información
                    </a>
                </div>
            ))}
        </div>
    </div>
</section>